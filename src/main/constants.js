export const OVERSCAN_COUNT = 1;
export const GRID_SYNC_ELEMENTS = Object.freeze([
  "#grid-header",
  "#grid-body",
  "#grid-virtual-scroller"
]);
export const TIMELINE_SYNC_ELEMENTS = Object.freeze([
  "#timeline-header",
  "#timeline-body",
  "#timeline-virtual-scroller"
]);
export const HEIRARCHY_PADDING = 12;
export const COLUMN_TYPES = Object.freeze({
  NAME: "name",
  PERCENT: "percent"
});
export const ICON_TYPES = Object.freeze({
  ARROW_DOWN: "arrow-down",
  ARROW_RIGHT: "arrow-right",
  DOT: "dot"
});
export const CELL_EXPAND_OR_COLLAPSE = "CELL_EXPAND_OR_COLLAPSE";
export const TASK_TYPES = Object.freeze(["parent", "task", "milestone"]);
